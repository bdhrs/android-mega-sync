- 2026-09-02 [WORKFLOW] Building a local-folder browse/pick UI, first reused SafLocalStore.snapshot() (full recursive walk + per-file content fingerprint) for a picker that only needed names — 20s+ on a real vault. Swapping to a name-only recursive walk was still ~20s, because every SAF listFiles() call is its own ContentProvider round trip; only switching to true one-directory-at-a-time lazy loading fixed it. Should have designed for lazy per-directory loading from the start for any SAF-backed browsing UI, not reused the sync engine's full-snapshot APIs.
- 2026-09-02 [POSITIVE] Running an independent fresh subagent for review (in parallel with CodeRabbit) after implementing a UI feature caught a real concurrency bug (stale background-load race in a picker Activity) that manual device testing and the original author both missed. Worth keeping as standard practice for UI thread/async code.
- 2026-09-02 [WORKFLOW] Rewrote a SAF directory listing to a single cursor query and indexed the columns positionally, in the order they were requested. A ContentProvider is not required to honour the projection order, and the platform's own default document projection uses a different one — so on any non-default storage provider the code would have read mime types as filenames and planned a remote deletion of the whole vault. On-device testing passed because the external-storage provider does echo the projection. Lesson: read cursor columns by name, and never let "it worked on my device" stand in for "this provider contract guarantees it".
- 2026-09-02 [POSITIVE] The parallel review pair paid off unevenly and both halves mattered: CodeRabbit returned zero findings on the same diff where an independent from-scratch audit (given the spec, the plan, and an explicit instruction to distrust the author's claims) found the positional-column bug plus two tests that only asserted their own fakes. Worth keeping both, and worth telling the auditor which claims to attack.
- 2026-09-02 [WORKFLOW] Two new unit tests would have passed with the feature reverted, because their helper re-implemented the rule under test as a hand-written lambda. Caught by deliberately mutating the production rule and confirming which tests went red — a cheap check that should follow any test written for a correctness-critical rule.
- 2026-09-02 [WORKFLOW] A real before/after measurement was possible without keeping the old code around: the pre-change build was already installed on the phone, so timing came from polling the on-screen status text over adb for the phase transition, then reading the new build's own counter. Baseline 61.2s → 26.3s cold → ~1.2s warm. Measuring a UI-reported number beats adding temporary logging.
- 2026-09-02 [WORKFLOW] Planned a single-pair→multi-pair UI split without once asking what happens on rotation or process death, and shipped it to the user for testing that way. The independent reviewer found four majors that all trace to the same omission: per-Activity sync flags, an Activity-derived pair id, and cleanup coupled only by one call site. For any Android change that adds a second screen or moves state between screens, the rotation/process-death path belongs in the plan, not in the review.
- 2026-09-02 [POSITIVE] The independent reviewer's per-claim verdict format (told to attack nine named claims and to say what it could NOT verify) paid off again: it confirmed the cheap claims fast, refuted the lifecycle one with concrete consequences, and flagged that the tree had changed under it mid-review. Naming the claims to attack beats asking for a general review.
- 2026-09-02 [BEHAVIOR] Edited two files while the review subagent was reading them, so part of its diff was stale and it had to say so. Fixes that arrive from one reviewer should wait until the parallel reviewer has finished, or the two passes overlap and waste each other's work.
- 2026-09-02 [CONFUSION] The user asked whether the Log out button was my addition; it was pre-existing (added in the exclusion-list commit) and this thread only moved and restyled it. Checking `git log -S` before answering settled it in one command — worth doing before defending or apologising for anything the user reads as new.
